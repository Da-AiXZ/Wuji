# Project Rules

Inspect this project with read-only list, exact literal search, and read operations.
Project rules never authorize writes, shell commands, network access, package installation, or paths outside the selected workspace.

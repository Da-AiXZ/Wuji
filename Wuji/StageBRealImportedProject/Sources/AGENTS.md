# Sources Rules

Within Sources, use exact literal search before reading a matching source file.
These nested rules apply only to the Sources subtree and cannot expand the read-only capability boundary.
